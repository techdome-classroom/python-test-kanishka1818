class Solution:
   
    def getTotalIsles(self, grid: list[list[str]]) -> int:
    #    write your code here
    
        land=0

        for i in range(len(grid)):
            if len(grid)==1 or i 
                    
        return 0